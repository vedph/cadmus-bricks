# Physical Measurement

🧱 A physical measurement. This essentially is a named [physical dimension](physical-dimension.md).

- name (`string`): the name of the measured property (e.g. width, weight, etc.).
- tag (`string`)
- value\* (`float`)
- unit\* (`string`)
